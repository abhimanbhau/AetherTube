diff --git a/appupdatechecker2/build.gradle b/appupdatechecker2/build.gradle
index 3a842bf..dcb94d7 100644
--- a/appupdatechecker2/build.gradle
+++ b/appupdatechecker2/build.gradle
@@ -1,67 +1,68 @@
-apply from: '../constants.gradle'
-apply plugin: 'com.android.library'
-
-android {
-    // FIX: Default interface methods are only supported starting with Android N (--min-api 24)
-    compileOptions {
-        sourceCompatibility JavaVersion.VERSION_1_8
-        targetCompatibility JavaVersion.VERSION_1_8
-    }
-
-    // allow other modules import debug/release configurations
-    //publishNonDefault true
-
-    compileSdkVersion project.properties.compileSdkVersion
-    buildToolsVersion project.properties.buildToolsVersion
-
-    defaultConfig {
-        minSdkVersion project.properties.minSdkVersion
-        targetSdkVersion project.properties.targetSdkVersion
-        versionCode 10
-        versionName "1.0.1"
-
-        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
-
-    }
-    
-    buildTypes {
-        release {
-            minifyEnabled false
-            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
-        }
-    }
-
-    lintOptions {
-        abortOnError true
-        disable 'MissingTranslation'
-        //lintConfig rootProject.file('lint.xml')
-    }
-
-    // gradle 4.6 migration: disable dimensions mechanism
-    // more: https://proandroiddev.com/advanced-android-flavors-part-4-a-new-version-fc2ad80c01bb
-    flavorDimensions "default"
-
-    productFlavors {
-        stbeta {}
-        ststable {}
-        stfdroid {}
-    }
-}
-
-dependencies {
-    androidTestImplementation('com.android.support.test.espresso:espresso-core:2.2.2', {
-        exclude group: 'com.android.support', module: 'support-annotations'
-    })
-    testImplementation 'junit:junit:' + junitVersion
-
-    // Removed melautils.jar - only StreamUtils was used, now provided as source
-    // See: src/main/java/edu/mit/mobile/android/utils/StreamUtils.java
-
-    implementation project(path: ':sharedutils')
-
-    implementation 'androidx.appcompat:appcompat:' + appCompatXVersion
-
-    implementation 'dnsjava:dnsjava:' + dnsjavaVersion
-
-    implementation 'com.squareup.okhttp3:okhttp:' + okhttpVersion
-}
+apply from: '../constants.gradle'
+apply plugin: 'com.android.library'
+
+android {
+
+    namespace 'com.liskovsoft.appupdatechecker2'
+    // FIX: Default interface methods are only supported starting with Android N (--min-api 24)
+    compileOptions {
+        sourceCompatibility JavaVersion.VERSION_1_8
+        targetCompatibility JavaVersion.VERSION_1_8
+    }
+
+    // allow other modules import debug/release configurations
+    //publishNonDefault true
+
+    compileSdkVersion project.properties.compileSdkVersion
+
+    defaultConfig {
+        minSdkVersion project.properties.minSdkVersion
+        targetSdkVersion project.properties.targetSdkVersion
+        versionCode 10
+        versionName "1.0.1"
+
+        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
+
+    }
+    
+    buildTypes {
+        release {
+            minifyEnabled false
+            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
+        }
+    }
+
+    lint {
+        abortOnError true
+        disable 'MissingTranslation'
+        //lintConfig rootProject.file('lint.xml')
+    }
+
+    // gradle 4.6 migration: disable dimensions mechanism
+    // more: https://proandroiddev.com/advanced-android-flavors-part-4-a-new-version-fc2ad80c01bb
+    flavorDimensions "default"
+
+    productFlavors {
+        stbeta {}
+        ststable {}
+        stfdroid {}
+    }
+}
+
+dependencies {
+    androidTestImplementation('com.android.support.test.espresso:espresso-core:2.2.2', {
+        exclude group: 'com.android.support', module: 'support-annotations'
+    })
+    testImplementation 'junit:junit:' + junitVersion
+
+    // Removed melautils.jar - only StreamUtils was used, now provided as source
+    // See: src/main/java/edu/mit/mobile/android/utils/StreamUtils.java
+
+    implementation project(path: ':sharedutils')
+
+    implementation 'androidx.appcompat:appcompat:' + appCompatXVersion
+
+    implementation 'dnsjava:dnsjava:' + dnsjavaVersion
+
+    implementation 'com.squareup.okhttp3:okhttp:' + okhttpVersion
+}
diff --git a/appupdatechecker2/src/main/AndroidManifest.xml b/appupdatechecker2/src/main/AndroidManifest.xml
index 92b5d7e..ff3b7a7 100644
--- a/appupdatechecker2/src/main/AndroidManifest.xml
+++ b/appupdatechecker2/src/main/AndroidManifest.xml
@@ -1,6 +1,5 @@
 <?xml version="1.0" encoding="utf-8"?>
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-    package="com.liskovsoft.appupdatechecker2">
+<manifest xmlns:android="http://schemas.android.com/apk/res/android">
 
     <uses-permission android:name="android.permission.INTERNET"/>
     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
diff --git a/build.gradle b/build.gradle
index 46fa4e1..70f8aed 100644
--- a/build.gradle
+++ b/build.gradle
@@ -4,7 +4,6 @@ buildscript {
     
     repositories {
         google()
-        jcenter()
     }
     dependencies {
         classpath 'com.android.tools.build:gradle:3.1.0'
@@ -18,7 +17,6 @@ buildscript {
 allprojects {
     repositories {
         google()
-        jcenter()
     }
 }
 
diff --git a/commons-io-2.8.0/build.gradle b/commons-io-2.8.0/build.gradle
index 42887b8..01249e5 100644
--- a/commons-io-2.8.0/build.gradle
+++ b/commons-io-2.8.0/build.gradle
@@ -2,6 +2,8 @@ apply from: '../constants.gradle'
 apply plugin: 'com.android.library'
 
 android {
+
+    namespace 'org.apache.commons.commonsio'
     // FIX: Default interface methods are only supported starting with Android N (--min-api 24)
     compileOptions {
         sourceCompatibility JavaVersion.VERSION_1_8
@@ -12,7 +14,6 @@ android {
     //publishNonDefault true
 
     compileSdkVersion project.properties.compileSdkVersion
-    buildToolsVersion project.properties.buildToolsVersion
 
     defaultConfig {
         minSdkVersion project.properties.minSdkVersion
@@ -31,7 +32,7 @@ android {
         }
     }
 
-    lintOptions {
+    lint {
         abortOnError true
         lintConfig rootProject.file('lint.xml')
     }
diff --git a/commons-io-2.8.0/src/main/AndroidManifest.xml b/commons-io-2.8.0/src/main/AndroidManifest.xml
index 2c01782..c4e6c98 100644
--- a/commons-io-2.8.0/src/main/AndroidManifest.xml
+++ b/commons-io-2.8.0/src/main/AndroidManifest.xml
@@ -1,2 +1 @@
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-          package="org.apache.commons.commonsio"/>
+<manifest xmlns:android="http://schemas.android.com/apk/res/android"/>
diff --git a/constants.gradle b/constants.gradle
index a09e1f0..001a085 100644
--- a/constants.gradle
+++ b/constants.gradle
@@ -2,7 +2,12 @@ project.ext {
     // ExoPlayer (Android Codec APIs) requires API 16+
     // Leanback requires API 17+
     // j2v8 6.2.1 requires API 18+
-    minSdkVersion = 17
+    // Compose for TV (androidx.tv:tv-material/tv-foundation) requires API 21+
+    // AetherTube raises the floor to 23 (Android 6.0): upstream SmartTube's very low floor forces
+    // several deps to stay on ancient versions purely for Android 4.x/5.x support. API 23+ covers
+    // effectively all Android TV hardware still in use (cheap boxes ship 7.1/9/11) at the cost of
+    // dropping Android TV 5.x and Fire OS 5 (Fire TV Stick 1st/2nd gen).
+    minSdkVersion = 23
     // Fix AppSettingsActivity crash: "Only fullscreen opaque activities can request orientation".
     // Error happen only on Android 8.0 (api 26) when you set activity orientation in manifest.
     // Possible fix is to set targetSdkVersion to 26.
@@ -20,28 +25,33 @@ project.ext {
     // - Backup/restore can access to the external directory
     // - Disabled auto start of the background service (e.g. remote control service)
     targetSdkVersion = 27 // 29 latest with lecacy storage access, 27 - latest with mouse dpad emulaton
-    compileSdkVersion = 34
-    buildToolsVersion = '30.0.3'
+    compileSdkVersion = 36
     //gradleVersion = '6.9.1' // https://developer.android.com/studio/releases/gradle-plugin#updating-gradle
     //androidGradleVersion = '4.0.0+' // last version compatible with IntellJ 2020.3
     
     testSupportVersion = '0.5'
     testXSupportVersion = '1.1.0'
-    kotlinVersion = '1.8.10' // latest for android-gradle 4.0.0 https://mvnrepository.com/artifact/org.jetbrains.kotlin/kotlin-stdlib-jdk7
-    kotlinxVersion = '1.7.3' // https://mvnrepository.com/artifact/org.jetbrains.kotlinx/kotlinx-coroutines-android
+    kotlinVersion = '2.0.21' // Compose compiler ships with Kotlin from 2.0 (compose-compiler-gradle-plugin)
+    kotlinxVersion = '1.8.1' // coroutines matching Kotlin 2.0
+    // Compose for TV (smarttubetv module only)
+    composeVersion = '1.10.3' // matches what androidx.tv 1.0.0 depends on; compiler now comes from Kotlin
+    tvComposeVersion = '1.0.0' // stable; drops the TvLazy* fork (modern Compose handles TV focus natively)
+    activityComposeVersion = '1.9.3'
+    // material-icons-core/extended stopped tracking the Compose version (frozen at 1.7.x upstream)
+    composeMaterialIconsVersion = '1.7.8'
     // NOTE: Kotlin core 1.7.0 has 'resource android:attr/lStar not found' error (dialog black background bug)
-    kotlinCoreXVersion = '1.12.0' // latest for sdk 17. https://mvnrepository.com/artifact/androidx.core/core-ktx
-    coreXVersion = '1.9.0' // the latest compatible with kotlin core and cronet 119
-    appCompatXVersion = '1.2.0' // old: 1.0.0
+    kotlinCoreXVersion = '1.15.0' // core-ktx; needs to carry OnUserLeaveHintProvider for activity/fragment 1.9+
+    coreXVersion = '1.15.0'
+    appCompatXVersion = '1.6.1' // 1.7.0+ is built with Kotlin 1.9 (metadata our 1.8.10 compiler rejects)
     leanbackXVersion = '1.0.0'
     viewpagerXVersion = '1.0.0'
-    activityXVersion = '1.0.0'
+    activityXVersion = '1.9.3'
     loaderXVersion = '1.0.0'
     interpolatorXVersion = '1.0.0'
     cardviewXVersion = '1.0.0'
     legacySupportV4XVersion = '1.0.0'
-    annotationXVersion = '1.1.0'
-    recyclerviewXVersion = '1.1.0'
+    annotationXVersion = '1.7.1'
+    recyclerviewXVersion = '1.3.2'
     preferenceXVersion = '1.1.0' // Note: 1.2.0 Requires multiple androidx.lifecycle libs
     mediaXVersion = '1.2.0' // exoplayer fix
     constraintLayoutXVersion = '1.1.2'
@@ -58,7 +68,7 @@ project.ext {
     checkerframeworkVersion = '2.5.0'
     testRunnerVersion = '1.0.2'
     // materialVersion = '1.2.1'
-    materialVersion = '1.5.0' // MaterialPicker dark style fix
+    materialVersion = '1.9.0' // 1.10+ is built with Kotlin 1.9 (metadata our 1.8.10 compiler rejects)
     flexboxVersion = '3.0.0'
     
     legacyExoplayerVersion = '2.9.6'
@@ -91,8 +101,11 @@ project.ext {
     
     multiDexVersion = '2.0.1' // crashlytics fix on Android 4.4
     stethoVersion = '1.5.0' // Chrome Dev Tools
-    glideVersion = '4.11.0'
-    webpDecoderVersion = '2.0.' + glideVersion // should end with glide version
+    glideVersion = '4.16.0' // 4.14+ required by glide-compose
+    // NOTE: the webpdecoder prefix tracks Glide's major line (2.0.x for Glide 4.11, 2.6.x for 4.16),
+    // so this can't be derived from glideVersion the way it used to be.
+    webpDecoderVersion = '2.6.4.16.0'
+    glideComposeVersion = '1.0.0-beta01'
     rxAndroidVersion = '2.1.1'
     rxJavaVersion = '2.2.21'
     diskLruCacheVersion = '2.0.2'
@@ -140,7 +153,7 @@ project.ext {
     netcipherVersion = '2.1.0'
     
     florianingerlRegexVersion = '1.1.11'
-    webkitVersion = '1.11.0' // Latest with Android 4.4 support
+    webkitVersion = '1.12.1' // 1.13+ requires compileSdk 35
     nanojsonVersion = '1.7'
     conscryptVersion = '2.5.3'
 }
diff --git a/j2v8/build.gradle b/j2v8/build.gradle
index e2835a4..9f52ed3 100644
--- a/j2v8/build.gradle
+++ b/j2v8/build.gradle
@@ -2,8 +2,9 @@ apply from: '../constants.gradle'
 apply plugin: 'com.android.library'
 
 android {
+
+    namespace 'com.eclipsesource.v8'
     compileSdkVersion project.ext.compileSdkVersion
-    buildToolsVersion project.ext.buildToolsVersion
     testOptions.unitTests.includeAndroidResources = true
 
     compileOptions {
diff --git a/j2v8/src/main/AndroidManifest.xml b/j2v8/src/main/AndroidManifest.xml
index 03847bd..3779503 100644
--- a/j2v8/src/main/AndroidManifest.xml
+++ b/j2v8/src/main/AndroidManifest.xml
@@ -1,5 +1,5 @@
 <?xml version="1.0" encoding="utf-8"?>
-<manifest package="com.eclipsesource.v8">
+<manifest>
 
     <application />
 
diff --git a/sharedtests/build.gradle b/sharedtests/build.gradle
index ade85f1..ffa6316 100644
--- a/sharedtests/build.gradle
+++ b/sharedtests/build.gradle
@@ -1,42 +1,43 @@
-apply from: '../constants.gradle'
-apply plugin: 'com.android.library'
-
-android {
-    compileSdkVersion project.ext.compileSdkVersion
-    buildToolsVersion project.ext.buildToolsVersion
-    testOptions.unitTests.includeAndroidResources = true
-
-    compileOptions {
-        sourceCompatibility JavaVersion.VERSION_1_8
-        targetCompatibility JavaVersion.VERSION_1_8
-    }
-
-    defaultConfig {
-        minSdkVersion project.ext.minSdkVersion
-        targetSdkVersion project.ext.targetSdkVersion
-        versionCode 1
-        versionName "1.0"
-
-        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
-
-    }
-
-    buildTypes {
-        release {
-            minifyEnabled false
-            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
-        }
-    }
-
-    lintOptions {
-        abortOnError true
-        lintConfig rootProject.file('lint.xml')
-    }
-}
-
-dependencies {
-    implementation fileTree(dir: 'libs', include: ['*.jar'])
-
-    testImplementation 'junit:junit:' + junitVersion
-    testImplementation 'org.robolectric:robolectric:' + robolectricVersion
-}
+apply from: '../constants.gradle'
+apply plugin: 'com.android.library'
+
+android {
+
+    namespace 'com.liskovsoft.sharedtests'
+    compileSdkVersion project.ext.compileSdkVersion
+    testOptions.unitTests.includeAndroidResources = true
+
+    compileOptions {
+        sourceCompatibility JavaVersion.VERSION_1_8
+        targetCompatibility JavaVersion.VERSION_1_8
+    }
+
+    defaultConfig {
+        minSdkVersion project.ext.minSdkVersion
+        targetSdkVersion project.ext.targetSdkVersion
+        versionCode 1
+        versionName "1.0"
+
+        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
+
+    }
+
+    buildTypes {
+        release {
+            minifyEnabled false
+            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
+        }
+    }
+
+    lint {
+        abortOnError true
+        lintConfig rootProject.file('lint.xml')
+    }
+}
+
+dependencies {
+    implementation fileTree(dir: 'libs', include: ['*.jar'])
+
+    testImplementation 'junit:junit:' + junitVersion
+    testImplementation 'org.robolectric:robolectric:' + robolectricVersion
+}
diff --git a/sharedtests/src/main/AndroidManifest.xml b/sharedtests/src/main/AndroidManifest.xml
index 996a9e9..876eded 100644
--- a/sharedtests/src/main/AndroidManifest.xml
+++ b/sharedtests/src/main/AndroidManifest.xml
@@ -1,2 +1 @@
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-          package="com.liskovsoft.sharedtests"/>
+<manifest xmlns:android="http://schemas.android.com/apk/res/android"/>
diff --git a/sharedutils/build.gradle b/sharedutils/build.gradle
index 28f9cce..18b1aa6 100644
--- a/sharedutils/build.gradle
+++ b/sharedutils/build.gradle
@@ -1,85 +1,92 @@
-apply from: '../constants.gradle'
-apply plugin: 'kotlin-android'
-apply plugin: 'com.android.library'
-
-android {
-    compileSdkVersion project.ext.compileSdkVersion
-    buildToolsVersion project.ext.buildToolsVersion
-    testOptions.unitTests.includeAndroidResources = true
-
-    compileOptions {
-        sourceCompatibility JavaVersion.VERSION_1_8
-        targetCompatibility JavaVersion.VERSION_1_8
-    }
-
-    defaultConfig {
-        minSdkVersion project.ext.minSdkVersion
-        targetSdkVersion project.ext.targetSdkVersion
-        versionCode 3
-        versionName "1.3"
-
-        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
-
-    }
-
-    buildTypes {
-        release {
-            minifyEnabled false
-            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
-        }
-    }
-
-    lintOptions {
-        abortOnError true
-        lintConfig rootProject.file('lint.xml')
-    }
-}
-
-dependencies {
-    implementation fileTree(dir: 'libs', include: ['*.jar'])
-
-    testImplementation 'junit:junit:' + junitVersion
-    testImplementation 'org.robolectric:robolectric:' + robolectricVersion
-    testImplementation project(path: ':sharedtests')
-    testImplementation project(path: ':sharedutils')
-
-    implementation project(path: ':commons-io-2.8.0')
-
-    implementation 'androidx.appcompat:appcompat:' + appCompatXVersion
-    // Note: 1.2.0 Requires multiple androidx.lifecycle libs
-    implementation 'androidx.preference:preference:' + preferenceXVersion
-
-    implementation 'com.squareup.okhttp3:okhttp:' + okhttpVersion
-    implementation 'com.squareup.okhttp3:okhttp-dnsoverhttps:' + okhttpVersion
-    implementation 'com.squareup.okhttp3:logging-interceptor:' + okhttpVersion
-    implementation 'com.squareup.okhttp3:okhttp-brotli:' + okhttpBrotliVersion
-    implementation('com.localebro:okhttpprofiler:' + okhttpProfilerVersion) {
-        exclude group: 'com.squareup.okhttp3', module: 'okhttp' // force global version
-    }
-
-    // Both libraries should be present.
-    // Use specific version of Cronet instead of one hardcoded inside okhttp extension
-    // implementation 'com.google.android.gms:play-services-cronet:' + playServicesCronetVersion
-    implementation 'org.chromium.net:cronet-api:' + cronetApiVersion
-    
-    // Cronet provider support (native, gms).
-    implementation 'org.chromium.net:cronet-embedded:' + cronetApiVersion
-
-    implementation 'com.google.code.gson:gson:' + gsonVersion
-
-    implementation 'io.reactivex.rxjava2:rxandroid:' + rxAndroidVersion
-    implementation 'io.reactivex.rxjava2:rxjava:' + rxJavaVersion
-    implementation 'com.jakewharton:disklrucache:' + diskLruCacheVersion
-
-    // Add modern TLS ciphers to HttpUrlConnection
-    // https://stackoverflow.com/questions/16299531/how-to-override-the-cipherlist-sent-to-the-server-by-android-when-using-httpsurl
-    implementation 'info.guardianproject.netcipher:netcipher:' + netcipherVersion
-
-    // Add kotlin support libs
-    implementation 'androidx.core:core-ktx:' + kotlinCoreXVersion
-    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:' + kotlinVersion
-
-    //implementation 'org.conscrypt:conscrypt-android:2.5.2'
-
-    implementation 'dnsjava:dnsjava:' + dnsjavaVersion
-}
+apply from: '../constants.gradle'
+apply plugin: 'kotlin-android'
+apply plugin: 'com.android.library'
+
+android {
+
+    buildFeatures {
+
+        buildConfig true
+
+    }
+
+    namespace 'com.liskovsoft.sharedutils'
+    compileSdkVersion project.ext.compileSdkVersion
+    testOptions.unitTests.includeAndroidResources = true
+
+    compileOptions {
+        sourceCompatibility JavaVersion.VERSION_1_8
+        targetCompatibility JavaVersion.VERSION_1_8
+    }
+
+    defaultConfig {
+        minSdkVersion project.ext.minSdkVersion
+        targetSdkVersion project.ext.targetSdkVersion
+        versionCode 3
+        versionName "1.3"
+
+        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
+
+    }
+
+    buildTypes {
+        release {
+            minifyEnabled false
+            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
+        }
+    }
+
+    lint {
+        abortOnError true
+        lintConfig rootProject.file('lint.xml')
+    }
+}
+
+dependencies {
+    implementation fileTree(dir: 'libs', include: ['*.jar'])
+
+    testImplementation 'junit:junit:' + junitVersion
+    testImplementation 'org.robolectric:robolectric:' + robolectricVersion
+    testImplementation project(path: ':sharedtests')
+    testImplementation project(path: ':sharedutils')
+
+    implementation project(path: ':commons-io-2.8.0')
+
+    implementation 'androidx.appcompat:appcompat:' + appCompatXVersion
+    // Note: 1.2.0 Requires multiple androidx.lifecycle libs
+    implementation 'androidx.preference:preference:' + preferenceXVersion
+
+    implementation 'com.squareup.okhttp3:okhttp:' + okhttpVersion
+    implementation 'com.squareup.okhttp3:okhttp-dnsoverhttps:' + okhttpVersion
+    implementation 'com.squareup.okhttp3:logging-interceptor:' + okhttpVersion
+    implementation 'com.squareup.okhttp3:okhttp-brotli:' + okhttpBrotliVersion
+    implementation('com.localebro:okhttpprofiler:' + okhttpProfilerVersion) {
+        exclude group: 'com.squareup.okhttp3', module: 'okhttp' // force global version
+    }
+
+    // Both libraries should be present.
+    // Use specific version of Cronet instead of one hardcoded inside okhttp extension
+    // implementation 'com.google.android.gms:play-services-cronet:' + playServicesCronetVersion
+    implementation 'org.chromium.net:cronet-api:' + cronetApiVersion
+    
+    // Cronet provider support (native, gms).
+    implementation 'org.chromium.net:cronet-embedded:' + cronetApiVersion
+
+    implementation 'com.google.code.gson:gson:' + gsonVersion
+
+    implementation 'io.reactivex.rxjava2:rxandroid:' + rxAndroidVersion
+    implementation 'io.reactivex.rxjava2:rxjava:' + rxJavaVersion
+    implementation 'com.jakewharton:disklrucache:' + diskLruCacheVersion
+
+    // Add modern TLS ciphers to HttpUrlConnection
+    // https://stackoverflow.com/questions/16299531/how-to-override-the-cipherlist-sent-to-the-server-by-android-when-using-httpsurl
+    implementation 'info.guardianproject.netcipher:netcipher:' + netcipherVersion
+
+    // Add kotlin support libs
+    implementation 'androidx.core:core-ktx:' + kotlinCoreXVersion
+    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:' + kotlinVersion
+
+    //implementation 'org.conscrypt:conscrypt-android:2.5.2'
+
+    implementation 'dnsjava:dnsjava:' + dnsjavaVersion
+}
diff --git a/sharedutils/src/main/AndroidManifest.xml b/sharedutils/src/main/AndroidManifest.xml
index e632462..46ec807 100644
--- a/sharedutils/src/main/AndroidManifest.xml
+++ b/sharedutils/src/main/AndroidManifest.xml
@@ -1,5 +1,4 @@
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-          package="com.liskovsoft.sharedutils">
+<manifest xmlns:android="http://schemas.android.com/apk/res/android">
     <uses-permission android:name="android.permission.BLUETOOTH"/>
 
     <!-- Android 11+: fix access package info -->
